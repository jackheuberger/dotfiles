-- stylua: ignore
return {
    {
        "nvim-neo-tree/neo-tree.nvim",
        config = {
            filesystem = {
                filtered_items = {
                    hide_gitignored = false,
                },
            },
        },
    },
}
